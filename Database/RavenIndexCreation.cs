using System.Linq;
using Raven.Client.Documents.Indexes;
using Raven.Client.Documents.Queries;

namespace Raven.Database
{
/*    /// <summary>Allow guilds to via GuildId. 
    /// Since this is our unique indentifier, this method will be how we get most of our queries.</summary>
    class RavenGuildById : AbstractIndexCreationTask<RavenGuild>
    {
        public RavenGuildById()
        {

        }
    }*/
}
